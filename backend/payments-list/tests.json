{
  "tests": [
    {
      "name": "OPTIONS preflight",
      "method": "OPTIONS",
      "path": "/",
      "expectedStatus": 200
    },
    {
      "name": "No token → 401",
      "method": "GET",
      "path": "/",
      "expectedStatus": 401,
      "expectedBody": { "error": "Требуется авторизация. Передайте JWT в заголовке Authorization: Bearer <token>" },
      "bodyMatcher": "partial"
    },
    {
      "name": "Invalid token → 401",
      "method": "GET",
      "path": "/",
      "headers": { "Authorization": "Bearer invalid.token.here" },
      "expectedStatus": 401,
      "bodyMatcher": "partial"
    }
  ]
}
